package {{.Pkg}}

import (
	"context"

	domain "{{.Module}}/internal/domain/{{.Pkg}}"
)

// Service holds {{.Pkg}} use cases.
type Service struct {
	repo domain.Repository
}

// NewService creates a new {{.Pkg}} Service.
func NewService(repo domain.Repository) *Service {
	return &Service{repo: repo}
}

// List returns all {{.Plural}}.
func (s *Service) List(ctx context.Context) ([]domain.{{.Type}}, error) { return s.repo.List(ctx) }

// Get returns a single {{.Pkg}} by ID.
func (s *Service) Get(ctx context.Context, id int64) (*domain.{{.Type}}, error) {
	return s.repo.Get(ctx, id)
}

// Create persists a new {{.Pkg}}.
func (s *Service) Create(ctx context.Context, e *domain.{{.Type}}) error { return s.repo.Create(ctx, e) }
