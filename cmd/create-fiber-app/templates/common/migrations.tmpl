package migrations

import "github.com/rahmadafandi/fiber-helpers/migrate"

// Migrations is the project migration collection. Migration files register
// themselves via init(). The directory is used by "migrate create" (run from
// the project root).
var Migrations = migrate.NewMigrations(migrate.WithMigrationsDirectory("internal/migrations"))
